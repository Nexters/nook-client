platform :ios, '15.0'
use_frameworks!

install! 'cocoapods', :disable_input_output_paths => true

# 네이티브 셸은 시스템 프레임워크(WebKit)만 사용 — Capacitor/Pod 없음.
# (ShareExtension 타깃도 Pod 미사용)
target 'App' do
end

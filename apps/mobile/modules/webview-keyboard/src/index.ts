// 이 모듈은 JS 로 부를 함수가 없다 — 네이티브 쪽 `OnCreate` 가 앱 시작 시 한 번
// WKWebView 의 키보드 액세서리 바 제거를 스위즈링으로 설치한다(ios/NookWebViewKeyboardModule.swift).
// 아무것도 export 하지 않지만, 이 파일이 있어야 로컬 Expo 모듈로 인식된다(`nook-session`과 동일 구조).
export {};
